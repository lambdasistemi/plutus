{
  "name": "uplc-browser-cek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:wasm": "mkdir -p src/assets && nix build ..#wasm-uplc -o result-wasm-uplc && rm -f src/assets/uplc.wasm && cp result-wasm-uplc/uplc.wasm src/assets/uplc.wasm",
    "clean": "rm -rf dist",
    "copy:index": "cp index.html dist/index.html",
    "build:js": "mkdir -p dist && esbuild src/bootstrap.js --bundle --format=iife --global-name=UplcBrowserCek --outfile=dist/bootstrap.js --loader:.wasm=binary",
    "build": "npm run prepare:wasm && npm run clean && npm run build:js && npm run copy:index",
    "serve": "node scripts/serve-dist.mjs 4173 dist",
    "test": "playwright test"
  },
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "^0.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "esbuild": "^0.27.1"
  }
}
